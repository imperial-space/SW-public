using Robust.Client.UserInterface;
using Content.Client.Message;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.Imperial.Medieval.Trading;

[GenerateTypedNameReferences]
public sealed partial class TradingStatsEntry : Control
{
    public TradingStatsEntry(string name, float reputation)
    {
        RobustXamlLoader.Load(this);
        EntryLabel.SetMarkup(Loc.GetString("trading-ui-stats-entry",
            ("name", name),
            ("reputation", reputation.ToString("0.0"))));
    }

}
