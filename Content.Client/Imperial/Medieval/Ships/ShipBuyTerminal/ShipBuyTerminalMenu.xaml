<DefaultWindow
    xmlns="https://spacestation14.io"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    Title="{Loc 'ship-buy-terminal-ui-title'}"
    MinSize="450 350"
    SetSize="550 450">
    <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True" Margin="4">
        <BoxContainer Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,6">
            <RichTextLabel
                Name="BalanceInfo"
                HorizontalExpand="True"
                VerticalAlignment="Center" />
            <Button
                Name="WithdrawButton"
                MinWidth="80"
                HorizontalAlignment="Right"
                Text="{Loc 'store-ui-default-withdraw-text'}" />
        </BoxContainer>
        <PanelContainer VerticalExpand="True">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#000000FF" />
            </PanelContainer.PanelOverride>
            <ScrollContainer
                HScrollEnabled="False"
                HorizontalExpand="True"
                VerticalExpand="True">
                <BoxContainer
                    Name="OffersContainer"
                    Orientation="Vertical"
                    HorizontalExpand="True"
                    VerticalExpand="True"
                    Margin="4" />
            </ScrollContainer>
        </PanelContainer>
    </BoxContainer>
</DefaultWindow>
